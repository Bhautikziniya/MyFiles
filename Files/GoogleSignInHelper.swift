//
//  GoogleSignInHelper.swift
//  Vachnamrut
//
//  Created by Bhautik Ziniya on 9/14/17.
//  Copyright © 2017 Agile Infoways. All rights reserved.
//

import UIKit

class GoogleSignInHelper: NSObject {
    /*
    private static var sourceViewController : UIViewController!
    
    // Check login status
    static var isUserLoggedIn: Bool {
        return GIDSignIn.sharedInstance().hasAuthInKeychain()
    }
    
    // Signin
    static func signIn(_ viewController : UIViewController) {
        sourceViewController = viewController
        GIDSignIn.sharedInstance().signIn()
    }
    
    // Sign Out
    static func signOut() {
        GIDSignIn.sharedInstance().signOut()
    }
    
    //MARK:Google SignIn Delegate
    func sign(inWillDispatch signIn: GIDSignIn!, error: Error!) {
        // myActivityIndicator.stopAnimating()
    }
    
    // Present a view that prompts the user to sign in with Google
    func sign(_ signIn: GIDSignIn!,
              present viewController: UIViewController!) {
        GoogleSignInHelper.sourceViewController.present(viewController, animated: true, completion: nil)
    }
    
    // Dismiss the "Sign in with Google" view
    func sign(_ signIn: GIDSignIn!,
              dismiss viewController: UIViewController!) {
        GoogleSignInHelper.sourceViewController.dismiss(animated: true, completion: nil)
    }
     */
}
